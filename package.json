{
  "name": "medication-adherence-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Medication Adherence Tracker monorepo",
  "scripts": {
    "test:backend": "npm test --prefix backend",
    "build:frontend": "npm run build --prefix frontend"
  }
}
